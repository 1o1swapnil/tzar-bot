"""tzar-bot — scope-safe penetration-testing automation toolkit.

The Python tools in this package are stdlib-first; optional feature deps
(PDF/DOCX/browser) are declared as extras in pyproject.toml. The single-source
version below is what pyproject reads via [tool.setuptools.dynamic].
"""

__version__ = "0.1.0"
